<?php
/* 서버 설정 — 이 파일을 config.php 로 복사해 채운다.
 *
 * config.php 는 저장소에 올리지 않는다(.gitignore 에 넣어 두었다).
 * 접속 정보를 메신저나 메일로 주고받지 말 것.
 */

return [

    /* 가비아 DB 접속 정보.
     * 가비아 My가비아 → 웹호스팅 → 관리 → DB 정보에서 확인한다. */
    'db' => [
        'host'   => 'localhost',
        'name'   => '여기에_DB이름',
        'user'   => '여기에_DB계정',
        'pass'   => '여기에_DB비밀번호',
        'charset'=> 'utf8mb4',
    ],

    /* 본인확인 해시에 섞는 문구.
     *
     * 관리자 화면에서 "서버 업로드본 내보내기" 를 누를 때 물어보는 값과
     * 글자 하나까지 같아야 조회가 된다. 다르면 아무도 조회되지 않는다.
     *
     * 길고 아무 뜻 없는 문자열로 정한다. 정한 뒤에는 바꾸지 않는다.
     * 바꾸면 이미 올린 자료의 해시가 전부 맞지 않게 되어 다시 올려야 한다. */
    'verify_salt' => '여기에_긴_임의문자열_최소32자',

    /* 관리자 업로드 열쇠.
     * upload.php 로 자료를 올릴 때 쓴다. 이것도 길게 정한다. */
    'upload_token' => '여기에_다른_긴_임의문자열',

    /* 조회를 허용할 주소.
     * 홈페이지가 GitHub Pages 에 있는 동안에는 둘 다 열어 둔다. */
    'allow_origins' => [
        'https://www.jllawfirm.co.kr',
        'https://jllawfirm.co.kr',
        'https://kingjin77-rgb.github.io',
    ],

    /* 한 곳에서 짧은 시간에 몇 번까지 조회할 수 있는지.
     * 이름과 생년월일은 찍어서 맞힐 수 있으므로 반드시 막아야 한다. */
    'rate' => [
        'window_min' => 10,   // 이 시간(분) 동안
        'max_fail'   => 8,    // 실패가 이만큼 쌓이면 잠시 막는다
        'max_total'  => 40,   // 성공을 포함한 전체 시도 상한
    ],
];
